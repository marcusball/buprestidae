<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>Blog</title>
    </head>

    <body>
        <h1>Hello</h1>

        {{#each posts}}
        <article>
            <h2><a href="{{url}}">{{title}}</a></h2>
            <div>
                {{{body}}}
            </div>
        </article>
        {{/each}}
    </body>
</html>